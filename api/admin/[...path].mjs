// Explicit Vercel route for admin API endpoints.
// Keep a dedicated catch-all for authentication and the admin dashboard API.
export { default } from "../../artifacts/api-server/dist-serverless/index.js";
